export const metadata = {
  title: "Tools · BlueBottleCap",
  description:
    "Convert, compress, merge, and split your files. PNG ↔ JPG ↔ WebP, image compressor, PDF merger, PDF splitter, PDF to images — all in your browser.",
};

export default function ToolsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
